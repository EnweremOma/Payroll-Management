export interface SalaryInput {
  firstName: string;
  lastName: string;
  level: string;
  employeeId: string;
  month: string;
  paidDays: string;
  basic: string;
  transport: string;
  pension: string;
  tax: string;
  total: string;
}